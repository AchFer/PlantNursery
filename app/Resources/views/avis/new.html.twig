{% extends 'front.html.twig' %}

{% block body %}
    <h1>Avi creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
    <a href="{{ path('avis_new') }}">add comment</a>
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('avis_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
