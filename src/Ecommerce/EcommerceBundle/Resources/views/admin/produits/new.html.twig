{% extends '@EcommerceEcommerce/layoutadmin.html.twig' %}

{% block body %}
    <h1>Produit creation</h1>


        {{ form_start(form, {'action': path('admin_new'), 'method': 'POST'}) }}
        {{ form_widget(form) }}
    {{form_errors(form.image)}}
<button type="submit" >Create</button>
        {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
