{% extends 'base.html.twig' %}

{% block body %}
    <h1>Userplant</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ userplant.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ userplant.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('userplant_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('userplant_edit', { 'id': userplant.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
